// ── Tipos compartidos ──────────────────────────────────────────────

export interface Product {
  id: number;
  name: string;
  tagline: string;
  price: string;
  weight: string;
  badge: string;
  imgA: string;
  imgB: string;
}

export interface NavItem {
  label: string;
  id: string;
}
